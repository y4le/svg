# Decision log

This is a compact chronological log. `proposed` entries are recommendations,
not owner-approved facts. Change their status to `accepted` when the owner
confirms them or implementation deliberately locks them in. To reverse an
accepted choice, append a superseding entry rather than rewriting its original
rationale.

| ID   | Date       | Status   | Decision                                                                                                                                                          | Rationale                                                                                                                                                                                                                   |
| ---- | ---------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D001 | 2026-08-04 | accepted | Keep the product core a code-forward SVG workbench with robust animation; treat GodSVG-class visual-first editing as a separate possible extension                | The owner explicitly separated the visual-first roadmap from the code-forward display; split preview alone is crowded, but folding a full vector editor into the core would bury the strongest source/animation loop        |
| D002 | 2026-08-04 | accepted | Treat the source string as the only save/export authority                                                                                                         | DOM serialization would silently rewrite user code and destroy the tool's trust proposition                                                                                                                                 |
| D003 | 2026-08-04 | accepted | Support CSS and SVG animation elements plus a shared inspection scrubber in MVP; defer arbitrary JavaScript and a visual keyframe timeline                        | Declarative animation is portable and can run in secure animated mode; two browser playback adapters can seek it without turning the product into a timeline authoring suite                                                |
| D004 | 2026-08-04 | accepted | Use CodeMirror 6 with its Lezer XML tree plus native DOMParser                                                                                                    | CodeMirror is focused and mobile-capable, and parser node ranges directly serve source/render mapping; DOMParser supplies browser XML semantics                                                                             |
| D005 | 2026-08-04 | accepted | Map authored parser nodes to preview DOM structurally per valid source version                                                                                    | This keeps mapping IDs out of source, preserves exact ranges, and allows honest per-element degradation                                                                                                                     |
| D006 | 2026-08-04 | accepted | Use CSS custom properties as the first variable substrate                                                                                                         | They remain valid portable SVG/CSS and cover the high-value numeric, time, color, and geometry cases without a template language                                                                                            |
| D007 | 2026-08-04 | accepted | Lead numeric controls with exact values and scrubbing; show sliders only for explicitly bounded variables                                                         | It preserves precision and follows the quiet, typographic Yale interaction ethos while retaining sliders where perceptual range matters                                                                                     |
| D008 | 2026-08-04 | accepted | Start with a static local-first Vite/TypeScript app, no UI framework, and no backend                                                                              | The validated scaffold establishes the static TypeScript boundary; CodeMirror, iframe preview, and playback are imperative owners, so framework/server complexity remains deferred until measured need                      |
| D009 | 2026-08-04 | accepted | Use one living design, one current plan, one decision log, dated research, and one concise agent policy                                                           | The baseline and architecture gate successfully carried context across sessions and collaborators without scale-dependent issue/ADR/review ceremony                                                                         |
| D010 | 2026-08-04 | accepted | Express core controls and any later visual extension through versioned source-range commands, never a second serialized project model                             | This preserves unsupported SVG and human formatting outside each explicit edit while leaving a safe extension seam                                                                                                          |
| D011 | 2026-08-04 | accepted | Treat GodSVG as the benchmark for a separate optional visual-editing extension, not as an implementation dependency or core roadmap                               | The owner named GodSVG as the desired capability endpoint and then explicitly placed visual-first editing in a separate possible extension; its scope and Godot model should not pull the core into becoming a vector suite |
| D012 | 2026-08-04 | accepted | Route variable and supported animation writes through one version-checked `EditIntent` compiler with explicit numeric formatting and byte-fidelity fixtures       | The implementation architecture gate locked one shared minimal-edit and undo path to prevent offset races, float noise, duplicated writeback paths, and later extension lockout without adding visual-editor features now   |
| D013 | 2026-08-04 | accepted | Preserve the original file bytes when unedited; retain BOM and uniform EOL after edits; visibly normalize mixed EOL after the first edit                          | CodeMirror uses a logical line model and cannot preserve arbitrary mixed separators internally, so the file envelope must make the transport policy truthful rather than promise impossible byte identity                   |
| D014 | 2026-08-04 | accepted | Build preview content by importing the validated XML DOM into a trusted script-disabled sandbox shell, never by putting user text through `srcdoc` or `innerHTML` | HTML reparsing would change case, namespaces, and entity behavior after validation; DOM import keeps rendered structure aligned with the indexed XML while CSP and sandboxing block active capabilities                     |
| D015 | 2026-08-04 | accepted | Use direct-root `@control` comments as the single MVP schema for bounded custom-property controls                                                                 | Comments are readable, independently diagnosable, source-range friendly, and semantically inert; optimizer loss only removes slider bounds and never the CSS value or document behavior                                     |
| D016 | 2026-08-04 | accepted | Enumerate document animations through `Document.getAnimations()` and make entering Interact mode resume the mixed CSS/SMIL clocks                                 | Document enumeration covers the preview without a fictitious subtree option, and paused transitions would otherwise make hover/click interaction appear broken                                                              |
| D017 | 2026-08-04 | accepted | Spell control directive names without the CSS `--` prefix and expand them when resolving a variable                                                               | XML comments cannot legally contain a double hyphen, so `@control radius` is the valid inert spelling for the `--radius` custom property                                                                                    |
| D018 | 2026-08-05 | accepted | Expose Tailnet development on a preflighted dedicated HTTPS port with a root route, while keeping ordinary Vite on loopback with a strict port                    | Serve strips path mounts, breaking Vite root-relative modules and HMR; a dedicated port preserves base `/`, and preflight restores the guard bypassed by root-only `--force --allow-root`                                   |
